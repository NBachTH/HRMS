# FaceZ frontend environment template.
# Copy this file to `.env.local` and adjust values for your setup:
#   cp .env.example .env.local

# Base URL of the backend API (Spring Boot runs on port 8084, context-path /face-z).
# Override if your backend host/port differs.
NEXT_PUBLIC_API_BASE=http://localhost:8084/face-z
